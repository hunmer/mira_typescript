{
  "name": "@hunmer/mira-monorepo",
  "version": "1.0.0",
  "description": "Mira TypeScript project - monorepo with core, server, and plugins",
  "private": true,
  "workspaces": [
    "*",
    "../plugins/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:core": "npm install && npm run build --workspace=mira-core",
    "build:server": "npm install && npm run build --workspace=mira-server",
    "build:storage": "npm install && npm run build --workspace=mira-storage-sqlite",
    "build:plugins": "npm install && npm run build --workspace=D:/mira_typescript/plugins/plugins/mira_thumb",
    "start:server": "npm run start --workspace=mira-server",
    "dev:server": "npm run dev --workspace=mira-server",
    "build:docker": "./mira-server/docker-build.bat",
    "clean": "Remove-Item *\\dist -Recurse -Force -ErrorAction SilentlyContinue; Remove-Item *\\node_modules -Recurse -Force -ErrorAction SilentlyContinue; Remove-Item ..\\plugins\\*\\dist -Recurse -Force -ErrorAction SilentlyContinue; Remove-Item ..\\plugins\\*\\node_modules -Recurse -Force -ErrorAction SilentlyContinue",
    "install-all": "npm install && npm install --workspaces"
  },
  "keywords": ["mira", "typescript", "server", "plugins"],
  "author": "hunmer",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2"
  }
}
